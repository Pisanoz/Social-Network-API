const {thought} = require('../models');

const thoughtController = {
// need the following methods getAllThoughts, getSingleThought, createThought, updateThought, deleteThought, addReaction, deleteReaction

}

module.exports =  thoughtController